{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "contracts",
  "root": "packages/contracts",
  "sourceRoot": "packages/contracts/src",
  "projectType": "library",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "outputs": ["{projectRoot}/dist"],
      "options": { "command": "tsc -p packages/contracts/tsconfig.build.json" }
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": { "command": "tsc -p packages/contracts/tsconfig.json --noEmit" }
    }
  }
}
