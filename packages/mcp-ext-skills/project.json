{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "mcp-ext-skills",
  "root": "packages/mcp-ext-skills",
  "sourceRoot": "packages/mcp-ext-skills/src",
  "projectType": "library",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "outputs": ["{projectRoot}/dist"],
      "options": { "command": "tsc -p packages/mcp-ext-skills/tsconfig.build.json" }
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": { "command": "tsc -p packages/mcp-ext-skills/tsconfig.json --noEmit" }
    }
  }
}
