/** Outlined wordmark stays identical before and after web fonts load. */
export function RachetLogo({ className }: { className?: string }) {
  return <img src="/brand/rachet-logo.svg" alt="Rachet" width={188} height={40} className={className} />;
}
